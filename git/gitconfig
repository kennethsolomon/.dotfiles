[init]
    defaultbranch = main
[user]
    email = hello@kennethsolomon.com
    name = kenneth solomon
    signingkey = 44AE23DAECB23F9C8702A0F7B2AC6F9ABA19FBE8
[gpg]
    program = /opt/homebrew/bin/gpg
[credential]
    usehttppath = true
    helper = osxkeychain
[commit]
    gpgsign = true

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n and N to move between diff sections
    side-by-side = true


    # delta detects terminal colors automatically; set one of these to disable auto-detection
    # dark = true
    # light = true

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
